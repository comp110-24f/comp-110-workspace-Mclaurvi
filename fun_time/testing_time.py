def blep(repetitions: int) -> str:
    """gives blep faces with the number put in being the number of faces"""
    return ":P " * repetitions


blep(repetitions=3)


def multiply(num: int, times: int) -> float:
    return num * times


print(multiply(num=1, times=repetitions))
